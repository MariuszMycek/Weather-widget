import React from 'react';

import './WidgetLoadingError.scss';

const WidgetLoadingError = () => (
  <div className="widget-loading-error">Sorry, we cannot get cities list</div>
);

export default WidgetLoadingError;
